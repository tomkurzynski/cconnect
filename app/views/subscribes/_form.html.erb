<%= form_for(subscribe) do |f| %>
  <% if subscribe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscribe.errors.count, "error") %> prohibited this subscribe from being saved:</h2>

      <ul>
      <% subscribe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%#= f.label :email %>
    <%= f.text_field :email, class:"width-50" %>
  </div>
<br><br>
  <div class="actions">
    <%= f.submit "Confirm", class:"width-50 w3-button w3-teal w3-hover-black" %>
  </div>
<% end %>
