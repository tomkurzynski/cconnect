 <div class="w3-container w3-padding-64 w3-center" id="team">
  <p id="notice"><%= notice %></p>
  
  <h1>Orders</h1>
  
  <table  class="w3-table-all w3-hoverable w3-mobile ">
    <thead>
      <tr>
        <th>Order date</th>
        <th>User</th>
        <th>Order status</th>
        <th colspan="3"></th>
      </tr>
    </thead>
  
  <!--OLD STYLING-->
    <!--<tbody>-->
    <!--  <% @orders.each do |order| %>-->
    <!--    <tr>-->
    <!--      <td><%= order.order_date %></td>-->
    <!--      <td><%= order.user.email%></td>-->
    <!--      <td><%= order.status %></td>-->
    <!--      <td><%= link_to 'Show', order %></td>-->
    <!--      <td><%= link_to 'Edit', edit_order_path(order) %></td>-->
    <!--      <td><%= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
    <!--    </tr>-->
    <!--  <% end %>-->
    <!--</tbody>-->
    
    <tbody>
      <% @orders.each do |order| %>
        <tr style="hover">
          <td><%= order.order_date %></td>
          <td><%= order.user.email%></td>
          <td><%= order.status %></td>
          <td><%= link_to '<i class="fas fa-eye fa-lg" title="View"></i>'.html_safe, order %></td>
          <td>
            <% if order.status == 'Pending' %>
              <%= link_to '<i class="fas fa-edit fa-lg" title="Edit"></i>'.html_safe, edit_order_path(order) %>
            <% end %>
            </td>
          <td>
            <% if order.status == 'Pending' %>
                <%= link_to '<i class="fas fa-trash-alt fa-lg" title="Destroy
                
                "></i>'.html_safe, order, method: :delete, data: { confirm: 'Are you sure?' } %>
              <% end %>
            </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  
  <br>
  

  </div>
  <!--<%= link_to 'New Order', new_order_path %>-->